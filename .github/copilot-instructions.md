# Copilot Instructions

See `AGENTS.md` at the repo root — that file is the router for all context, rules, and day-by-day guidance for this project. This file exists only as a thin pointer for Copilot surfaces that look in `.github/` specifically.
