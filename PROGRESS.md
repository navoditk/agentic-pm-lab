# Progress: Agentic AI Learning Journey

This file is largely **auto-generated**. `scripts/check_progress.py`, run by `.github/workflows/progress-tracker.yml` on every push to `main`, regenerates everything between the `<!-- PROGRESS:START -->` / `<!-- PROGRESS:END -->` markers below from `config/progress.yaml` and from `# MOCK` markers found in `src/`. Don't hand-edit inside the markers — edit `config/progress.yaml` instead, and let the next push regenerate the table. The one-line narrative entries and the "Notes" column are the only parts a person writes directly.

See `docs/PLAN.md` §6 (Automatic progress tracking) for how this mechanism works, and `AGENTS.md` for how to onboard a tool onto the current day's work.

<!-- PROGRESS:START -->

## Status: Day 12 of 20

**Environment setup:** ✅ `INSTALL.md` complete. *(This checkbox is the one line in this file that isn't auto-generated by `progress-tracker.yml` — it's flipped to ✅ by hand, as the last item in `INSTALL.md`'s own verification checklist, since that mechanism doesn't exist until Day 1 builds it.)*

*(Days 10–20 are the forward institutional PM track. Days 13–14 are now mainstream milestones; the older optional-extension wording in `docs/PLAN.md` is historical.)*

**Day-by-day completion:**

| Day | Focus | Status | Notes |
|---|---|---|---|
| 1 | Foundation: walking skeleton | ✅ Complete |  |
| 2 | Data Layer: real public data | ✅ Complete |  |
| 3 | Tool Layer: real deterministic engines | ✅ Complete |  |
| 4 | Deep Agents: single agent | ✅ Complete |  |
| 5 | Deep Agents: multi-agent orchestration | ✅ Complete |  |
| 6 | OpenTelemetry, LangSmith, eval dataset | ✅ Complete |  |
| 7 | Control Layer for real | ✅ Complete |  |
| 8 | Canvas fundamentals | ✅ Complete |  |
| 9 | Canvas: Agent Operations | ✅ Complete |  |
| 10 | Canvas: Portfolio/Risk capstone | ✅ Complete |  |
| 11 | Runtime & Automation | ✅ Complete |  |
| 12 | AWS Bedrock AgentCore, wrap-up | ✅ Complete |  |
| 13 | AgentCore Memory & Evaluations | ⬜ Not started |  |
| 14 | Bedrock Guardrails + stretch | ⬜ Not started |  |
| 15 | Point-in-time data and provenance | ⬜ Not started |  |
| 16 | SEC research and multimodal evidence | ⬜ Not started |  |
| 17 | AWS investment-research pattern | ⬜ Not started |  |
| 18 | Devil's Advocate and committee challenge | ⬜ Not started |  |
| 19 | Production AgentOps and Canvas | ⬜ Not started |  |
| 20 | Institutional PM capstone | ⬜ Not started |  |

**Mock → real status, by layer:**

| Layer | Status | Detail |
|---|---|---|
| Data Layer | 🟡 Partial | 3/4 file(s) real |
| Control Layer (AuthN/AuthZ) | 🟢 Real | 6 file(s), no `# MOCK` markers |
| Guardrails | 🟢 Real | 1 file(s), no `# MOCK` markers |
| Tool Layer | 🟡 Partial | 8/10 file(s) real |
| Portfolio Optimization | 🟢 Real | 1 file(s), no `# MOCK` markers |
| Interactive Layer | 🟢 Real | 68 file(s), no `# MOCK` markers |
| Runtime Layer | 🟢 Real | 2 file(s), no `# MOCK` markers |
| Agent Layer | 🟢 Real | 5 file(s), no `# MOCK` markers |
| Observability | 🟢 Real | 2 file(s), no `# MOCK` markers |
| Golden dataset / evals | 🟢 Real | 4 file(s), no `# MOCK` markers |
| AWS Bedrock AgentCore | 🔴 Not started | No files yet |

<!-- PROGRESS:END -->

## Daily narrative log

*(One short entry per day, written by hand alongside the auto-generated table above — what happened, any deviation from docs/PLAN.md, anything worth flagging for tomorrow, and evidence links: PR, test run, eval run, trace, screenshot, ADR — whichever apply to that day. Full retrospective detail belongs in `docs/LEARNINGS.md`, not here — this is a log line, not a journal entry.)*

- **Day 1:** Walking skeleton built across every layer -- mock DuckDB data layer, allowlist/audit Control Layer stub, six FastAPI Tool Layer stubs, local artifact host, `ci.yml`/`progress-tracker.yml`/`skills-freshness.yml` (placeholder), `example-echo` and `python-best-practices` skills, `.pre-commit-config.yaml` (ruff, detect-secrets, no-company-sensitive-data, check-yaml/json, commitizen), `config/progress.yaml` + `scripts/check_progress.py` wired to `PROGRESS.md`, 15 passing unit tests, and this project's first `docs/ARCHITECTURE.md`. Deviation from plan: added `.gitignore` and pytest's `pythonpath`/`--import-mode=importlib` config, neither called out explicitly in docs/PLAN.md but both needed for `uv run pytest` to resolve `src.*` imports and for multiple skills sharing the `tests/test_skill.py` filename to coexist. Evidence: `tests/unit/` (15 passing), `uv run pre-commit run --all-files` (all green), `PROGRESS.md`'s own auto-generated table above now reads Day 1 Complete. Still open: GitHub Projects (v2) board setup is a browser step, not yet done.
- **Day 2:** Public yfinance ETF prices and FRED macro/Treasury observations now populate TTL-cached DuckDB tables; `/tools/curve` reads the real eight-tenor Treasury curve while the security master and portfolio positions remain explicitly mocked. Added the mock-migration and glossary skills, three initial FICC definitions, and the user-scoped `ficc-tutor-agent`. Evidence: commits `c39b1f6`, `49bfd60`, `4116526`, and `be29dae`; `tests/unit/ingestion/` (10 passing).
- **Day 3:** Replaced Tool Layer stubs with deterministic pricing, interpolation, portfolio, risk, econometrics, and backtest engines; added seven JSON Schema contracts, reusable timeout/malformed-response fixtures, identity/role checks with audit logging at every tool route, and the `new-tool-onboarding` skill. Research remains intentionally mocked, and portfolio classifications still depend on the mock security master. Evidence: commits `aa6c784`, `0b92bab`, `c2f689a`, `21e2ec3`, and `ab27302`; analytics/API/control tests cover formulas, contracts, and allowed/denied boundaries.
- **Day 4:** Added named full/filtered context composition, a nine-tool single Deep Agent, skill-creator/skill-tester meta-skills, the three-read-only-tool `portfolio-risk-summary` skill, fake-model routing tests, and an optional Ollama/Qwen3 4B variant. OpenAI was selected instead of Anthropic; its initial call was blocked by `insufficient_quota`, then a `gpt-4.1-mini` smoke test succeeded after credits were added. Local transcript: "portfolio volatility" → `get_volatility(returns, window=2, periods_per_year=252)` → `[null, 0.2245, 0.1684, 0.0786]`, and "largest concentration" → `get_portfolio_exposure(...)` → 60% with the mock-classification disclosure. The overload experiment cut tokens but Qwen3 skipped the tool on a 500-return payload, recorded as a real limitation rather than a pass. Evidence: commits `8518262`, `fcf5dfa`, `121008f`, `817cbb4`, `74e281f`, and `909c2f2`; `docs/comparison-notes.md`.
- **Day 5:** Added a native Deep Agents Portfolio Manager with Macro, Quant/Risk, and Fundamental specialists, strict domain tool sets, the Day 5 `scenario-analysis` stub, exact-name tool-contract validation, bounded retry/backoff and loop limits, explicit dead-letter results, and checkpoint/resume. A real `gpt-4.1-mini` run delegated rates to Macro and volatility to Quant; it exposed a dropped annualization parameter that was then hardened in prompts/tests. Injected baselines showed a timeout aborting the whole workflow and malformed pricing being accepted; recovery tests now reject both and prove `macro=1` while a crashed Quant goes from one to two invocations on resume. The optional Qwen3 4B hierarchy returned empty without delegating, recorded as a limitation. Evidence: commits `8fa2059`, `383d86c`, `9960ab2`, `9e9d0f8`, and `868bc9f`; `docs/ARCHITECTURE.md` and `docs/comparison-notes.md`.
- **Day 6:** Added one OpenTelemetry pipeline across FastAPI, analytics, agents, authorization, and audit operations, with LangSmith as the OTel agent-reasoning view and cost/token/latency attributes on agent spans. Built 15 active golden/routing cases, independent evaluators, an OTel-native LangSmith experiment runner, versioned fast/full baselines, regression CI, and a read-only eval-triage agent. The accepted full `gpt-4.1-mini` baseline scored routing/retrieval context 100%, tool selection/arguments 86.7%, and final-answer criteria 46.7%, using 167,946 tokens, an estimated $0.0774, and 363.4 seconds; policy and guardrail dimensions remain explicitly unscored until Days 7 and 12. Evidence: commits `73a1ed3`, `6c36d81`, `a16aba7`, `5b08208`, and `91ae730`; experiment `day6-full-5bcd4d5c`; `docs/observability-evaluation.md`.
- **Day 7:** Split local authentication from authorization: `config/roles.yaml` now maps only `PM_USER`, `RISK_USER`, and `ADMIN_USER`, while Cedar exclusively controls known tools and `PORT_A`/`PORT_B`. Agent construction filters tools by identity, portfolio context is checked before model access, FastAPI repeats tool/resource checks, the shared denied-term guardrail blocks input/context/output, and `run_backtest` genuinely pauses through `interrupt_on`. Role-gating tests prove PM (`price-bond`/`PORT_A` allowed, unknown write/`PORT_B` denied, backtest interrupted), Risk (`risk`/`PORT_B` allowed, pricing/unknown portfolio denied), and Admin (`research`/`PORT_B` allowed, unknown tool/portfolio denied); all six allowed/denied records carry 32-character OTel trace IDs. Role spoofing, portfolio bypass, output exfiltration, and disguised-write cases are blocked. The 18-case full experiment activated policy compliance at 100% without lowering Day 6's fast behavioral floors. Evidence: commits `5ef077a`, `011e243`, `f1e97a6`, `b3534f2`, `faf45b9`, `7ef1a35`, `b169277`, and `d98e871`; experiment `day7-full-450f21c2`; `governance/tests/` and `tests/unit/control/test_role_gating.py`.
- **Day 8:** Added the canvas-capability-authoring skill, then shipped `agentic-kanban` and `issue-triage-canvas` as shared-state canvases with the same handlers available to both the agent and UI. `agentic-kanban` proved the create/assign/move pattern; `issue-triage-canvas` pulled real GitHub issues, filtered/prioritized them, and kept network access inside SDK-free handlers. Added the `docs-agent` custom agent and mocked capability tests for both canvases. Evidence: commits `9f971d4`, `60abca4`, `ce201eb`, and `94ca433`; smoke tests for both canvases and the canvas-capability test files under `.github/extensions/`.
- **Day 9:** Built `agent-ops-canvas` as the operational surface over the seeded Day 4/5/6/7 run history: run lists, traces, guardrails, cost metrics, a paused approval run, and a side-by-side comparison panel for single-agent versus multi-agent observations. `get_runs`, `get_trace`, `retry_node`, `approve_run`, `run_evaluation`, `get_guardrail_results`, and `get_cost_metrics` are all wired; the live LangSmith path shells out to `scripts/run_eval.py` and now surfaces a clear `LANGSMITH_API_KEY` requirement instead of pretending success. Evidence: commit `31df777`; smoke tests plus mocked capability tests for `agent-ops-canvas`; updated `docs/ARCHITECTURE.md`, `docs/LEARNINGS.md`, and `config/progress.yaml`.
- **Day 10:** Added the contract-backed MCP adapter at `src/mcp_server/`, delegating to the existing deterministic analytics and re-checking Cedar tool permission plus portfolio entitlement from MCP request metadata. The Portfolio/Risk Canvas now documents the governed MCP boundary, separates public curve data from mock holdings/scenario fixtures, and retains role-gated portfolio, scenario, trace, provenance, and approval capabilities. Added `risk-narrator-agent` with standalone examples and negative cases. Evidence: `tests/unit/mcp_server/` (contract registration, eight delegated tools, identity and cross-portfolio denial); Canvas smoke/capability tests; `docs/ARCHITECTURE.md`.
- **Day 11:** Added the local production path (`Dockerfile`, `docker-compose.yml`, API/MCP/artifact services, optional Streamlit comparison), a single-file risk-report generator, seven Copilot prompt files, `pr-reviewer-agent` and `skills-auditor-agent`, static skill-contract and freshness checks, contract/skill CI, approval-only weekday morning-brief automation, and `docs/RUNBOOK.md`. Evidence: 100 unit tests, 12-skill static contract check, freshness fixture tests, Ruff/pre-commit checks; the scheduled issue and native Copilot automation remain GitHub-account/browser evidence tasks.
- **Day 12:** Added deterministic rates/credit scenario analysis, constrained max-Sharpe/min-volatility/risk-parity allocation proposals, shared JSON contracts, FastAPI/MCP/Deep Agent wiring, optimization narration skill and prompt, AgentCore direct-code entrypoint, Runtime/Gateway/Identity/Policy/Guardrail/OTel deployment intent, and ADRs for direct-code deployment and the Gateway-only tool path. The local suite reaches 113 tests plus 13 authorization tests; no live AWS resource or trace is claimed until account setup, deployment, and teardown are explicitly performed. A PyPortfolioOpt/SciPy HRP compatibility fallback is documented in code and covered by tests.
