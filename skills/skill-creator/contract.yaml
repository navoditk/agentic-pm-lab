inputs:
  name:
    type: string
  description:
    type: string
allowed_tools:
  - apply_patch
forbidden_tools:
  - web_search
output_schema:
  type: object
  required:
    - skill_directory
side_effects: Creates a new package under skills/.
approval_required: false
covers:
  - scripts/validate_skill.py
version: 1.1.0
